# NEXT_PUBLIC_MAPBOX_TOKEN - Required for interactive map (get from https://account.mapbox.com/access-tokens)
# Create .env.local with: NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here
# If missing, the app shows a graceful fallback message instead of a blank map